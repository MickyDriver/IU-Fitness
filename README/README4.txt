DATACOLLECTION
requires use of firebase as database


createEvent
adds new appointments available to be viewed from the database and displayed

getEvents
gets all current events from the database and displays them

freeSlots
displays currently available slots by excluding occupied slots