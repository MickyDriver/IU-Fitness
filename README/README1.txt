In order to run the code, I used Visual Studio Code

You want to install any missing dependencies (npm install "" --save)
React, CSS, bootstrap, jquery popper

Point directory toward client folder (cd client)

Start the app (npm start)